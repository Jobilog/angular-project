import { CreatedTimeAgoPipe } from './created-time-ago.pipe';

describe('CreatedTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new CreatedTimeAgoPipe();
    expect(pipe).toBeTruthy();
  });
});
